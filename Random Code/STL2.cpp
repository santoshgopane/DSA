// DoublyEndedQueue (Dque), List
#include <iostream>
#include <typeinfo>
#include <deque>
#include <list>

using namespace std;
int main()
{
    /*
    DEQUE

    deque<int> d;

    d.push_back(1);
    d.push_front(10);
    d.push_front(4);
    cout << "Size: " << d.size() << endl;
    for (int i : d)
        cout << i << " ";
    cout << endl;
    d.pop_back();

    cout << "Size: " << d.size() << endl;
    for (int i : d)
        cout << i << " ";
    d.erase(d.begin(), d.end());
    cout << "Size: " << d.size() << endl;

    for (int i : d)
        cout << i << " ";
    */

    /*
    LIST

    list<int> l;
    l.push_back(1);
    l.push_back(2);
    l.push_front(3);
    // cout << "Size of List before: " << l.size() << endl;
    // cout << "test:" << *l.begin() << endl;
    // for (int i : l)
    //     cout << i << " ";
    // l.erase(l.begin());

    // cout << "Size of List after: " << l.size() << endl;
    // cout << endl;

    // for (int i : l)
    //     cout << i << " ";
    list<int> l2(l);
    for (int i : l2)
        cout << i << " ";
    */
}
