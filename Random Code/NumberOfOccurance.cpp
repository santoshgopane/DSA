#include <iostream>
#include <bits/stdc++.h>
using namespace std;

pair<int, int> numberOfOccurances(int arr[], int size)
{
    // sort(arr, arr + sizeof(arr) / sizeof(arr[0]));
    // cout << arr[0];
}

int main()
{
    int arr[99] = {8, 2, 3, 4, 5, 6};
    numberOfOccurances(arr, 6);
}